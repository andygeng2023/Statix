# Generic process definition for a host that supports long-running workers.
# Set the environment variables from .env.example in your host's secret manager.
command = "python scanner_worker.py"
restart = "always"
